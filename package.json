{
  "name": "example-search-techniques",
  "version": "1.0.0",
  "scripts": {
    "client": "cp .env app-ui && cd app-ui && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.2.1",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "dotenv": "10.0.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
